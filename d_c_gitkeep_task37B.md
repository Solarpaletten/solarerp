Отлично. Делаем 37B правильно — как ядро ERP.

Ниже — чистое ТЗ для Claude.

---

# 📘 TASK 37B — Purchase Items Table (Read View)

## 🎯 Цель

Реализовать **таблицу позиций документа Purchase**
на странице:

```
/company/[companyId]/purchases/[purchaseId]
```

Это Read-only версия.
Без редактирования.
Без сохранения.
Без inline input.

---

# 🏗 Архитектура

Создать:

```
components/purchases/PurchaseItemsTable.tsx
```

Подключить в:

```
PurchaseDocumentPage
```

Вместо placeholder блока 37B.

---

# 📦 Данные

Используем `purchase.items`, которые уже приходят из API.

Каждый item содержит:

```
id
itemName
itemCode
quantity
priceWithoutVat
vatRate
```

---

# 📋 Таблица должна содержать

Колонки:

1. #
2. Item Name
3. Code
4. Quantity
5. Price (net)
6. VAT %
7. Net Total
8. VAT Amount
9. Gross Total

---

# 🧮 Расчёты

Для каждой строки:

```
netTotal = quantity * priceWithoutVat
vatAmount = netTotal * (vatRate / 100)
grossTotal = netTotal + vatAmount
```

Если vatRate null → считать 0.

Все числа:

* формат de-DE
* 2 decimal places
* tabular-nums
* выравнивание вправо

---

# 🖥 UI Требования

* Белый фон
* Border
* Rounded-lg
* overflow-x-auto
* Sticky header (thead sticky top-0)
* Тонкая серая сетка
* text-xs
* Нет теней
* Нет input
* Нет кнопок

Это должен быть “бухгалтерский” вид.

---

# 📐 Поведение

* Если items.length === 0 → показать:
  "No items in this document"
* Если есть items → показать таблицу
* Нумерация строк начинается с 1

---

# 🔒 Read-only правило

Если статус документа:

```
CANCELLED
LOCKED
```

Таблица отображается без изменений (только визуально).

Никаких disabled пока не нужно.

---

# 📱 Responsive

Desktop:

* Полная таблица

Mobile:

* Горизонтальный scroll

---

# 🧪 Acceptance Criteria

* Нет console errors
* Нет NaN
* Нет undefined
* Все расчёты корректны
* Sticky header работает
* Scroll работает
* Числа правильно форматируются

---

# ❗ Не делать

* Не делать Add row
* Не делать Edit
* Не делать Delete
* Не делать inline input
* Не делать totals summary (это 37C)

---

# 🎯 Цель 37B

Сделать бухгалтерскую таблицу,
которая визуально ощущается как ERP,
а не как карточка UI.

---

Скопируй это Claude.

Когда будет код — делаем строгий Gate Review.
