D=>C

**Task 30 — Import German Chart of Accounts (SKR03) Engine**

Цель:
Создать полноценный импорт плана счетов (SKR03) в систему, чтобы ERP работала на реальной бухгалтерской структуре.

Без миграции.
Без изменений в schema.
Работаем поверх существующей модели `Account`.

---

# 🎯 Цель Task 30

Реализовать:

```
POST /api/company/[companyId]/chart-of-accounts/import
```

Импорт CSV → массовое создание счетов.

После выполнения:

* В компании должно быть 200–300+ счетов
* Дубли не создаются
* Ledger сразу может использовать реальные счета

---

# 🧱 Требования к CSV

Формат:

```csv
code,name,type
1000,Bank,ASSET
1200,Accounts Receivable,ASSET
1600,Input VAT 19%,ASSET
1776,Output VAT 19%,LIABILITY
8400,Sales 19%,INCOME
3400,Cost of Goods Sold,EXPENSE
```

type должен соответствовать enum:

* ASSET
* LIABILITY
* EQUITY
* INCOME
* EXPENSE

---

# 🔒 Backend Requirements

### 1️⃣ Tenant isolation

* Проверить ownership компании

### 2️⃣ Проверка CSV

* Заголовок обязателен
* type валидировать
* code обязателен
* name обязателен

### 3️⃣ Дубликаты

* Проверка по `code + companyId`
* Если уже существует → пропустить
* Не падать

### 4️⃣ Atomic behavior

Импорт через transaction.

---

# 📊 Response format

```json
{
  "created": 247,
  "skipped": 3,
  "totalInFile": 250
}
```

---

# 📁 Структура файлов

Создать:

```
app/api/company/[companyId]/chart-of-accounts/import/route.ts
```

Парсинг CSV через:

* встроенный split (достаточно для MVP)
  или
* lightweight parser (без тяжёлых библиотек)

---

# 🧠 Дополнительно

Добавить временную кнопку в UI:

Chart of Accounts page:

```
[ Import SKR03 ]
```

Кнопка отправляет CSV-файл.

---

# ❗ Важно

НЕ удаляем старые счета.
НЕ изменяем существующие.
НЕ делаем update-режим.

Это чистый add-import.

---

# 🧪 После импорта нужно проверить

1️⃣ GET /accounts
2️⃣ Убедиться, что счета появились
3️⃣ Trial Balance должен учитывать их
4️⃣ Можно создать проводку на 8400, 1776 и т.д.

---

# ⏱ Оценка

Backend: ~60–90 минут
Frontend кнопка: ~20–30 минут

---

D=>C

Сделать чистый импорт-движок.
Без лишней магии.
После реализации — прислать код.
