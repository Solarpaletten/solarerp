D=>C

**Task 29 — Trial Balance (MVP, инженерная версия 1.0)**
Дата старта: 27.02.2026
Цель: доказать математическую консистентность Ledger.

Работаем строго поверх `JournalEntry` / `JournalLine`.
Документы не участвуют.

---

# 🎯 Цель Task 29

Реализовать endpoint:

```
GET /api/company/[companyId]/reports/trial-balance
```

который:

* агрегирует Ledger
* возвращает обороты
* доказывает, что Σ Debit == Σ Credit

---

# 📌 Scope (MVP, без излишеств)

## Query params:

```
?from=YYYY-MM-DD
&to=YYYY-MM-DD
```

Оба обязательны.

---

# 📊 Что должен вернуть endpoint

Минимальный ответ:

```json
{
  "from": "2026-01-01",
  "to": "2026-01-31",
  "totalDebit": "10000.00",
  "totalCredit": "10000.00",
  "isBalanced": true
}
```

Важно:

* totals возвращать строкой (Decimal → toString())
* isBalanced = totalDebit === totalCredit

---

# 🧮 Логика расчёта

Использовать `journalLine` + join `entry`.

Фильтр:

```
entry.companyId = companyId
AND entry.date >= from
AND entry.date <= to
```

Агрегировать:

```
SUM(debit)
SUM(credit)
```

Через Prisma aggregate или groupBy — на твоё усмотрение,
но без raw SQL.

---

# 🔒 Обязательные требования

1. Tenant isolation

   * verifyCompanyOwnership
2. Валидация дат
3. Если from > to → 400
4. Если нет проводок → вернуть 0.00 / 0.00 / true
5. MANUAL участвует (ничего не фильтруем по source)

---

# 📁 Структура

Создать файл:

```
app/api/company/[companyId]/reports/trial-balance/route.ts
```

---

# 🧠 Архитектурный принцип

Ledger — единственный источник истины.

Никаких:

* документов
* stock
* bankStatement

Только Journal.

---

# 🧪 Smoke test после реализации

1️⃣ Создать SALE
2️⃣ Создать PURCHASE
3️⃣ Создать MANUAL
4️⃣ Запросить trial balance

Ожидаем:

```
isBalanced = true
```

---

# ⏱ Оценка

~60–90 минут аккуратной реализации.

---

D=>C

Задача понятна.
Сделай чистый MVP endpoint без лишнего.

После реализации пришли код.
Проверим, затем введём реальные цифры.
