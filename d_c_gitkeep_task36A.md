Отлично.
Вот теперь мы переходим на уровень «ERP, а не форма».

Эта верхняя панель — это не кнопки.
Это **Document Action Bar**.

Её нужно выделить как отдельную задачу.

---

# 🔵 TASK 36A

## Purchase Action Bar (верхняя полоса)

---

## 🎯 Цель

Реализовать универсальную панель действий для страницы:

```
/company/[companyId]/purchases
```

Панель должна управлять выбранными документами.

---

# 📦 Кнопки (слева направо)

| Кнопка | Действие                |
| ------ | ----------------------- |
| ➕      | Создать новый документ  |
| ✏️     | Редактировать выбранный |
| 🗑     | Удалить выбранные       |
| 🔒     | Закрыть документ        |
| 🔓     | Открыть документ        |
| 📄     | Копировать документ     |
| ⬇      | Импорт (пока disabled)  |

---

# 🧠 Логика

## 1️⃣ Выбор строк

В таблице должен быть:

```
[ ] checkbox
```

* Можно выбрать несколько
* Выбранные id хранятся в состоянии

```ts
const [selectedIds, setSelectedIds] = useState<string[]>([])
```

---

## 2️⃣ Поведение кнопок

### ➕ Create

Переход на:

```
/purchases/new
```

---

### ✏️ Edit

Работает если выбран 1 документ.

```
/purchases/[purchaseId]
```

Если выбрано 0 или >1 → disabled.

---

### 🗑 Delete

Работает если выбран ≥1.

Отправляет:

```
DELETE /api/company/[companyId]/purchases
body: { ids: string[] }
```

После — рефреш списка.

---

### 📄 Copy

Работает если выбран 1 документ.

POST:

```
/api/company/[companyId]/purchases/[id]/copy
```

Backend создаёт новый документ.

---

### 🔒 / 🔓

Пока только UI (backend можно подключить позже).

---

# 🎨 UI Требования

* Отдельный компонент:

```
components/purchases/PurchaseActionBar.tsx
```

* Фиксированная высота
* Светлый серый фон
* Кнопки иконками
* Disabled стиль если нельзя нажать
* Без popover пока

---

# 🧱 Архитектура

Страница Purchases должна выглядеть так:

```
PurchasesPage
 ├── PurchaseActionBar
 ├── PurchaseTable
```

---

# 🧪 Acceptance Criteria

Task выполнен если:

* Можно выбрать строки
* ActionBar реагирует на выбор
* Кнопки активируются корректно
* Delete реально удаляет
* Copy создаёт новый документ
* Нет ошибок в консоли

---

# ❗ Важно

Мы строим структуру, как в серьёзной ERP:

Сначала:

1. List
2. ActionBar
3. Document Page

Не смешиваем.

---

Ты сейчас строишь уже взрослую систему.

Если хочешь — я могу сразу описать Task 36B (Purchase Table proper)
чтобы Claude сделал всё по порядку.
